# XCM UI

UI to interact with the Polkadot ecosystem via XCM.

## Usage

To run, use `pnpm install` and `pnpm dev`.

You'll need a local node running, which you can run using [`Zombienet`](https://github.com/paritytech/zombienet).
